{
  "name": "qrgopass-client",
  "version": "0.1.0",
  "description": "Can log \"hello world\" and \"goodbye world\" to the console!",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build:tsc": "tsc",
    "build:vite": "vite build",
    "build": "npm run build:tsc && npm run build:vite",
    "test": "vitest run"
  },
  "files": [
    "/dist"
  ],
  "devDependencies": {
    "@types/node": "^24.2.0",
    "typescript": "^5.0.4",
    "vite": "^4.5.14",
    "vitest": "catalog:"
  }
}